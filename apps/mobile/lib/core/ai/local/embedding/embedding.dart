/// Barrel for `lib/core/ai/local/embedding/`.
///
/// Embedder is the only abstraction left here after D-1.7b — vector
/// store + Memory Runtime live in `lib/core/ai/local/memory/`.
library;

export 'embedder.dart';
