# Settings Overview UI

Settings landing-page sections live here. Keep `settings_page.dart` as the
route-level wrapper in `features/settings/ui/`; section composition and row
groups belong in this directory.
