# Income Planner UI

The Income Planner page and its local part files live here. The entrypoint stays
`income_planner_page.dart`; part files use local names such as `body.dart` and
`states.dart` because the directory supplies the page context.

Sibling sheets and label helpers that are reused by other Options Income pages remain in
`features/finance/options_income/ui/`.
