# Wave 41+ — allowlist of test files currently failing on `main`.
#
# Pinned by `tool/check-known-failing-tests.sh`. New PRs cannot
# introduce additional failing files; reducing this list is welcome.
# When a file here is fixed, remove its line (the script will warn).
#
# Baseline is currently zero. Keep this file as the documented place
# for an exceptional temporary entry; do not add one without reviewer
# sign-off and a brief reason.
#
# Add new lines ONLY with reviewer sign-off and a brief reason.
flow/navigation_flow_test.dart  Timer leak on dispose — pre-existing, not regressed by recent changes
flow/net_worth_flow_test.dart   Timer leak on dispose — same root cause as navigation_flow_test
