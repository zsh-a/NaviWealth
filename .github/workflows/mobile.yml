name: mobile

on:
  push:
    branches: [main]
    paths:
      - 'apps/mobile/**'
      - 'apps/backend/src/ai/**'
      - 'apps/backend/src/bin/tool_descriptor_dump.rs'
      - '.github/workflows/mobile.yml'
      - 'codecov.yml'
      - 'docs/ai-protocol.md'
      - 'tool/check-tool-descriptors.sh'
      - 'tool/check-modal-helpers.sh'
      - 'tool/fetch-onnxruntime.sh'
      - 'tool/embed-onnxruntime-android.sh'
      - 'tool/embed-onnxruntime-macos.sh'
      - 'apps/mobile/tool/check-back-nav-coverage.sh'
  pull_request:
    paths:
      - 'apps/mobile/**'
      - 'apps/backend/src/ai/**'
      - 'apps/backend/src/bin/tool_descriptor_dump.rs'
      - '.github/workflows/mobile.yml'
      - 'codecov.yml'
      - 'docs/ai-protocol.md'
      - 'tool/check-tool-descriptors.sh'
      - 'tool/check-modal-helpers.sh'
      - 'tool/fetch-onnxruntime.sh'
      - 'tool/embed-onnxruntime-android.sh'
      - 'tool/embed-onnxruntime-macos.sh'
      - 'apps/mobile/tool/check-back-nav-coverage.sh'

concurrency:
  group: mobile-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

defaults:
  run:
    working-directory: apps/mobile

jobs:
  analyze-and-test:
    name: analyze + test (coverage)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true
      - run: flutter --version
      - run: flutter pub get
      - name: Stub web-only font assets so test asset bundle resolves
        # All five files are gitignored build artifacts (see
        # tool/build-{cn,latin}-fonts.sh). flutter analyze + flutter test
        # don't actually rasterise glyphs; the asset bundler just needs
        # readable files at the registered paths. Empty stubs are enough.
        run: |
          mkdir -p assets/fonts
          touch assets/fonts/app-cn-base.woff2 assets/fonts/app-cn-ext.woff2
          touch assets/fonts/inter-regular.woff2 assets/fonts/inter-medium.woff2 \
                assets/fonts/inter-semibold.woff2 assets/fonts/inter-bold.woff2 \
                assets/fonts/outfit-bold.woff2
      - name: Check mobile tool descriptors match backend
        working-directory: ${{ github.workspace }}
        run: tool/check-tool-descriptors.sh
      - run: flutter analyze --fatal-infos
      - name: Lint — no Chinese literals in source (FIR-99)
        run: dart tool/check_cn_literals.dart
      - name: Verify generated code is up to date
        run: |
          dart run build_runner build --delete-conflicting-outputs
          if ! git diff --quiet -- '*.g.dart' '*.freezed.dart'; then
            echo "::error::Generated files are stale. Run 'dart run build_runner build --delete-conflicting-outputs' locally and commit the result."
            git --no-pager diff --stat -- '*.g.dart' '*.freezed.dart'
            exit 1
          fi
      - name: Schema-as-contract — Dart↔Rust enum mirror (Wave 42)
        # Catches "added a Rust variant but forgot the Dart mirror"
        # (and vice versa). The script diffs enums of the same name
        # across mobile/contracts and backend/ai/context.
        working-directory: ${{ github.workspace }}
        run: ./tool/check-enum-mirror.sh
      - name: Schema-as-contract — ARB l10n key parity (Wave 42)
        # Both en and zh must define exactly the same key set —
        # otherwise zh users see English at runtime via the silent
        # fallback path.
        run: ./tool/check-l10n-parity.sh
      - name: Unified modal system — no raw Material modals in features
        # Every sheet/dialog must go through the design-system helpers
        # (showAppSheet / showAppFormSheet / showConfirmDialog) so chrome
        # stays identical. Bans showDialog/AlertDialog/etc. in features.
        working-directory: ${{ github.workspace }}
        run: ./tool/check-modal-helpers.sh
      - name: Money display lint — no naked toStringAsFixed
        # Existing non-money uses are centrally allowlisted. Any new naked
        # formatter must either use MoneyText/AppFormatters or document a
        # non-money reason in tool/money-display-to-string-allowlist.txt.
        working-directory: ${{ github.workspace }}
        run: ./tool/lint-money-display.sh --strict
      - name: Back-nav coverage — every non-tab page surfaces a way out
        # Tab pages (Today/Activity/Wealth/Plan) intentionally have no
        # back arrow. Every other FHeader.nested page must wire one via
        # appSubPageHeader() or backHeaderAction() so users pushed in
        # via deep link / sibling nav can leave without the system
        # gesture (which doesn't exist on web).
        run: ./tool/check-back-nav-coverage.sh
      - name: Run tests + pin failure footprint (Wave 41)
        # We can't yet bring the failing-test footprint to zero (~50
        # files broken by design-system API drift unrelated to the AI
        # work). The gate below pins the footprint at its Wave 41
        # baseline so new red files can't sneak in. Reducing the
        # baseline is welcome; tool/known-failing-tests.txt explains.
        run: |
          set +e
          flutter test --coverage --reporter=expanded --exclude-tags=golden \
            | tee /tmp/test_out.log
          test_exit=${PIPESTATUS[0]}
          set -e
          ./tool/check-known-failing-tests.sh < /tmp/test_out.log
          echo "Test run completed (flutter exit=${test_exit}; baseline gate=PASS)"
      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v6
        with:
          files: apps/mobile/coverage/lcov.info
          flags: mobile
          token: ${{ secrets.CODECOV_TOKEN }}
          fail_ci_if_error: false
      - name: Upload coverage artifact
        uses: actions/upload-artifact@v4
        with:
          name: mobile-coverage
          path: apps/mobile/coverage/lcov.info
          if-no-files-found: error

  golden-regression:
    name: golden regression (mobile)
    runs-on: ubuntu-latest
    needs: analyze-and-test
    steps:
      - uses: actions/checkout@v4
      - uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true
      - run: flutter pub get
      - name: Build golden font assets
        run: |
          tool/build-cn-fonts.sh
          tool/build-latin-fonts.sh
      - name: Run mobile golden suite (FIR-113)
        # The visual baseline is locked to Linux rasterisation. Devs on
        # macOS / Windows skip the comparison via flutter_test_config.dart;
        # this job is the source of truth. A failed match dumps the diff
        # PNG next to the golden so it's recoverable from the artifact.
        run: flutter test test/golden --tags=golden --reporter=expanded
      - name: Upload golden diffs on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: mobile-golden-failures
          path: |
            apps/mobile/test/golden/failures/**
            apps/mobile/test/golden/goldens/**
          if-no-files-found: ignore
          retention-days: 7

  build-web:
    name: build web
    runs-on: ubuntu-latest
    needs: analyze-and-test
    steps:
      - uses: actions/checkout@v4
      - uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true
      - run: flutter pub get
      - name: Materialize drift web assets (sqlite3.wasm + drift_worker.dart.js)
        run: tool/setup-drift-web.sh
      - name: Build CN web font subsets (app-cn-base.woff2 + app-cn-ext.woff2)
        # Fails the build if app-cn-base.woff2 exceeds the 250 KB budget.
        run: tool/build-cn-fonts.sh
      - name: Build Latin font subsets (Inter + Outfit, FIR-104)
        # Fails the build if the combined Inter + Outfit subset exceeds
        # the 200 KB budget.
        run: tool/build-latin-fonts.sh
      - name: Read backend API URL
        id: api-url
        working-directory: ${{ github.workspace }}
        run: echo "url=$(cat apps/backend/.api-base-url | tr -d '\n')" >> "$GITHUB_OUTPUT"
      - run: flutter build web --release --source-maps --dart-define=API_BASE_URL=${{ steps.api-url.outputs.url }} --dart-define=GIT_SHA=${{ github.sha }}
      - uses: actions/upload-artifact@v4
        with:
          name: mobile-web-build
          path: apps/mobile/build/web
          retention-days: 7
          if-no-files-found: error

  deploy-pages:
    name: deploy Cloudflare Pages
    runs-on: ubuntu-latest
    needs: build-web
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    env:
      HAS_CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN != '' }}
    concurrency:
      group: pages-deploy-prod
      cancel-in-progress: false
    steps:
      - uses: actions/checkout@v4
        if: env.HAS_CLOUDFLARE_API_TOKEN == 'true'
      - name: Download mobile web artifact
        if: env.HAS_CLOUDFLARE_API_TOKEN == 'true'
        uses: actions/download-artifact@v8
        with:
          name: mobile-web-build
          path: apps/mobile/build/web
      - name: Skip Cloudflare Pages deploy
        if: env.HAS_CLOUDFLARE_API_TOKEN != 'true'
        run: echo "::notice::CLOUDFLARE_API_TOKEN not set in GitHub Secrets — skipping Pages deploy."
      - name: Deploy to Cloudflare Pages
        if: env.HAS_CLOUDFLARE_API_TOKEN == 'true'
        uses: cloudflare/wrangler-action@v4
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          workingDirectory: apps/mobile
          command: pages deploy --branch main
